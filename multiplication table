//Write a function to print the Table(needs to accept the input as number)

	func numTable(n1:Int)->Int{
    	for i in 1...10 {
        	let result = n1 * i
        	print(n1, "*", i, "=", result)
    	}
    	return 0
	}
	numTable(n1:52)
